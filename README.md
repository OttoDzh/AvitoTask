Получаем список сотрудников по запросу, парсим данные и представляем их в таблице в отсортированном виде.
We get a list of employees on request, parse the data and present them in a table in sorted form.
